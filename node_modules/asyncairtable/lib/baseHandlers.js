export const booleanHandler = (bool) => {
    return bool ? 'TRUE()' : 'FALSE()';
};
export const baseHandler = (val) => {
    if (val === null) {
        return 'BLANK()';
    }
    switch (typeof val) {
        case 'number':
            return `${val}`;
        case 'string':
            return `'${val}'`;
        case 'boolean':
            return booleanHandler(val);
    }
};
//# sourceMappingURL=baseHandlers.js.map