declare type RateLimitHandler = <T>(url: string, opts: RequestInit, retryTimeout: number, maxRetry: number, key?: string) => Promise<T>;
declare const rateLimitHandler: RateLimitHandler;
export default rateLimitHandler;
