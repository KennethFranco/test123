import { AsyncAirtable } from './asyncAirtable';
declare type Args = {
    endpoint: string;
    options?: RequestInit;
    instance: AsyncAirtable;
    pageHandler?: {
        index: number;
        page: number;
    };
};
export declare const request: <T>({ endpoint, options, instance, pageHandler, }: Args) => Promise<T>;
export {};
